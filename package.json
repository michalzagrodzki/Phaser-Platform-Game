{
  "name": "phaser3-project-a-1",
  "version": "1.0.0",
  "description": "Platform game based on official tutorial",
  "main": "index.js",
  "scripts": {
    "dev": "parcel watch src/index.html -p 2000",
    "build": "parcel build src/index.html --out-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michal Zagrodzki",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.23.0"
  }
}
